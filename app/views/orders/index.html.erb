<%= render partial: "pending_orders", locals: { pending_orders: pending_orders, current_user: current_user }%>
<%= render partial: "delivered_orders", locals: { delivered_orders: delivered_orders }%>
